### Security notes

M0.9 changes repository validation and CI/release mechanics only. It adds no secrets, personal data, service egress, runtime dependencies, infrastructure changes, or production writes. The release workflow grants `packages: write` only to the job that loads and pushes the image archive already built and scanned by the read-only job.

### Open questions

None.

### Deviations from plan

None.
