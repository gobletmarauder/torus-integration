# M6 PR notes

## Security notes

- No Terraform plan, import, apply, state operation, backend initialization, credential access, or Cloudflare API call was performed.
- Terraform manages only the reviewed Cloudflare resource allowlist. The existing Turnstile widget is imported, and critical DNS records, the widget, and the backups bucket have `prevent_destroy`.
- The saved-plan gate blocks removals, replacements, unapproved resource types, more than 30 changes, forbidden DNS ownership, broadened Access policies, and account/zone identifier mismatches.
- The repository guard independently permits only provably safe `ops` and `ssh` CNAME declarations. MX, TXT, NS, CAA, apex, `www`, and indeterminate DNS declarations fail.
- No credentials, account identifiers, zone identifiers, personal email addresses, state, tfvars, plan artifacts, or sensitive output values are committed or printed.

## Open questions

Rehaan must confirm that the manually created `torus-tfstate` bucket exists before the later credentialed backend initialization and provide no-default inputs through gitignored tfvars. The first saved plan must show the existing Turnstile widget as an import, pass `tf_gate.py`, and receive Claude review before apply.

## Deviations from plan

`docs/pr-notes/M6.md` was added because the merged M0.9 verification bundle contract requires milestone PR notes. It was inadvertently omitted from the approved M6 file list; the G0 plan was updated during implementation. This is a non-protected documentation-only scope correction and adds no dependency, host, privilege, external write, or protected path. There are no other deviations.
