# M4 PR notes

## Security notes

- Runtime egress adds only the exact Cloudflare Access team host approved at G0.
- The database keepalive uses `SELECT 1`; it adds no Supabase REST host, RPC, scope, or write.
- Environment examples and Compose fixtures contain names or synthetic values only. The real `.env` was never accessed or copied into this worktree.
- Access assertion values, signing keys, database connection strings, record identifiers, and personal data are not returned or logged.

## Open questions

None.

## Deviations from plan

None.
